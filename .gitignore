# Python-generated files (both sub-projects)
**/__pycache__/
**/*.py[oc]
**/*.egg-info/
**/build/
**/dist/
**/wheels/

# Virtual environments (each sub-project manages its own)
**/.venv/

# Tool caches
**/.mypy_cache/
**/.pytest_cache/
**/.ruff_cache/

# Reproducibly provisioned third-party source trees (see tau2_adapter setup).
/.deps/

# Workstation-only RTX 5090/Qwen runner. Keep it on the local machine; the
# shared adapter and NSCC deployment must not depend on this execution profile.
/tau2_qwen27b_local/

# inspect_trace's own derived JSONL output (only meaningful alongside a specific run's .eval log)
**/.inspect_trace/

# Runtime logs from local-model-server's serve.sh, and stray .eval logs that running
# inspect_trace's test suite / doc examples from inspect_trace/ writes to ./logs by default
local-model-server/logs/
inspect_trace/logs/
/logs/

# Experiment data: real .eval logs + inspect_trace JSONL from real benchmark runs.
# Not tracked -- it's large and regenerable, but NOT disposable: see runs/ in
# deployment_migration_guide.md, it must be copied by hand when migrating servers.
/runs/

# Same as /runs/ above, but specifically results pulled from the NSCC compute node (see
# nscc_runs/README.md for why this is a separate directory from /runs/). Keep the README tracked
# so the directory's purpose survives a fresh clone even before anyone has pulled anything into it.
/nscc_runs/*
!/nscc_runs/README.md
